<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Amine Hochlef</title>
<link>https://www.aminehochlef.github.io/blog.html</link>
<atom:link href="https://www.aminehochlef.github.io/blog.xml" rel="self" type="application/rss+xml"/>
<description>Amine Hochlef&#39;s humble writings</description>
<language>en</language>
<generator>quarto-1.6.39</generator>
<lastBuildDate>Sun, 05 Jan 2025 23:00:00 GMT</lastBuildDate>
<item>
  <title>DataCamp : Git Intermediate course notes [Collaborating with git]</title>
  <dc:creator>Amine Hochlef</dc:creator>
  <link>https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git-part-two/collaborating_with_git.html</link>
  <description><![CDATA[ 





<section id="merge-conflicts" class="level1">
<h1>Merge conflicts</h1>
<p>When merging two branches it’s not very incommon to have conflicts, a conflict means two branches getting merged have two different versions of a file for example and git doesn’t know which one to keep.</p>
<p>Therefore when we get a conflict we need to fix it in order to merge properly, but how is that?</p>
<p>When git generates a conflict, it writes “special characters” in the content of file to specify the problem, to resolve the conflict we remove the special syntax, and keep the changes made by the feature branch if we want to keep it, save the modified file, add it to the staging area, commit it, and then try mergin again.</p>
</section>
<section id="introduction-to-remotes" class="level1">
<h1>Introduction to remotes</h1>
<p>Remote repos are repos that are in the cloud this repos has many benefits like :</p>
<ul>
<li>Work is backed up</li>
<li>Teams can collaborate on the same project</li>
</ul>
<p>Companies can choose to not host their repos on the cloud if they have strict privacy rules regarding data.</p>
<section id="cloning-repos" class="level2">
<h2 class="anchored" data-anchor-id="cloning-repos">Cloning repos</h2>
<section id="cloning-local-repos" class="level3">
<h3 class="anchored" data-anchor-id="cloning-local-repos">Cloning local repos</h3>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> clone path/to/project</span></code></pre></div>
<p>We can also give it a name like this :</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> clone path/to/project new_repo_name</span></code></pre></div>
</section>
<section id="cloning-remote-repos" class="level3">
<h3 class="anchored" data-anchor-id="cloning-remote-repos">Cloning remote repos</h3>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> clone URL_of_repo</span></code></pre></div>
<p>When cloning remote repos git remembers where the original was, we can list all remotes associated with the repo like this :</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> remote</span></code></pre></div>
<p>or if we want to the result to be more detailed or “verbose”:</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb5-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> remote <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-v</span></span></code></pre></div>
<p>By default git will name the remote repo <code>origin</code>, we can name the remote ourselves by using this command :</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> remote add <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;</span>name of remote repo<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span> URL</span></code></pre></div>
<p>Example :</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> remote add hmed https://github.com/hmed/repo</span></code></pre></div>
</section>
</section>
</section>
<section id="pulling-from-remotes" class="level1">
<h1>Pulling from remotes</h1>
<p>Fetching from the <code>origin</code> remote to our local repo:</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> fetch origin </span></code></pre></div>
<p>This :</p>
<ul>
<li>Doesn’t merge the remote repo contents into our local repo</li>
<li>It might add new branches that are present in the remote but not in the local</li>
<li>It fetches <strong>ALL</strong> branches</li>
</ul>
<p>If we want to fetch a single remote branch :</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb9-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> fetch origin main</span></code></pre></div>
<p>To synchronize content after fetching, we use :</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb10-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> merge origin</span></code></pre></div>
<p>This merges origin with our current local branch</p>
<p>The <code>pull</code> command is more convenient it simplifies the operation by fetching and merging “at the same time”</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb11-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> pull origin</span></code></pre></div>
<p>As <code>fetch</code> we can pull a single branch from the remote :</p>
<div class="sourceCode" id="cb12" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb12-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> pull origin dev</span></code></pre></div>
</section>
<section id="pushing-to-remotes" class="level1">
<h1>Pushing to remotes</h1>
<p>To push changes to our remote repo from our local repo we use :</p>
<div class="sourceCode" id="cb13" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb13-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> push remote local</span></code></pre></div>
<p>Example :</p>
<div class="sourceCode" id="cb14" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb14-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> push origin main</span></code></pre></div>
<p>In a typical workflow we first pull changes from the remote repo, work on our changes locally, save, commit and then push to the remote repo, if we don’t pull first we might encounter an error when pushing stating that our local repo is behind and we need to pull from the remote before pushing.</p>


</section>

<a onclick="window.scrollTo(0, 0); return false;" id="quarto-back-to-top"><i class="bi bi-arrow-up"></i> Back to top</a> ]]></description>
  <category>course</category>
  <category>notes</category>
  <category>git</category>
  <guid>https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git-part-two/collaborating_with_git.html</guid>
  <pubDate>Sun, 05 Jan 2025 23:00:00 GMT</pubDate>
  <media:content url="https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git-part-two/course_complete_git.png" medium="image" type="image/png" height="75" width="144"/>
</item>
<item>
  <title>DataCamp : Git Intermediate course notes [Working with branches]</title>
  <dc:creator>Amine Hochlef</dc:creator>
  <link>https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git/working_with_branches.html</link>
  <description><![CDATA[ 





<section id="introduction-to-branches" class="level1">
<h1>Introduction to branches</h1>
<p>A branch is like a parallel universe of the git repo, by default the default branch of a git repo is main, if we create a new branch by doing :</p>
<div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> branch example-branch</span></code></pre></div>
<p>the new <code>example-branch</code> could have it’s own files and its own versions of files that are present in main, it’s really like a parallel universe!</p>
<p>Branches are useful when we want to add a new feature or to fix a bug, that way we create a branch called bug-fix (in the example of fixing a bug), and work on the project simultaneously without affecting the main project (we are branching-off), even if we have problems in our current project that won’t affect the live project because it’s getting served by the main branch.</p>
<p>We could switch between branches using :</p>
<div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> switch example-branch</span></code></pre></div>
<p>We could also create and switch to a new branch at the same time :</p>
<div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> switch <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-c</span> example-branch</span></code></pre></div>
<p>To check our branches :</p>
<div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> branch</span></code></pre></div>
<p>When we are okay with the result in the branch, now it’s time to merge back into the main branch, this is presented in this simple illustration :</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git/git-branches.png" class="img-fluid figure-img"></p>
<figcaption>Git branches</figcaption>
</figure>
</div>
</section>
<section id="modifying-and-comparing-branches" class="level1">
<h1>Modifying and comparing branches</h1>
<p>To compare two branches, like we compare two commits, or to compare file (staged or unstaged) to the latest commit etc … :</p>
<div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb5-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> diff main example-branch</span></code></pre></div>
<p>If we get into the trouble of confusing branch names, we need to resolve the issue by renaming the branches by a more clear name :</p>
<div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb6-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> branch <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-m</span> old-branch-name new-branch-name</span></code></pre></div>
<p>It’s good practice to delete the branch after finishing with it and merging with the main branch to do this we execute the following command :</p>
<div class="sourceCode" id="cb7" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb7-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> branch <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-d</span> example-branch</span></code></pre></div>
<p>If <code>example-branch</code> hasn’t been merged to <code>main</code> and we did the last command we will get an error, if we want to force the deletion :</p>
<div class="sourceCode" id="cb8" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb8-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> branch <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-D</span> example-branch</span></code></pre></div>
</section>
<section id="merging-branches" class="level1">
<h1>Merging branches</h1>
<p>When merging branches the branch that we want to merge from (the one with the new feature) is called the <code>source</code> branch, and the branch that we want to merge into is called the <code>destination</code> branch, the last commits from each branch are called the <code>parent commits</code>, if we want to merge <code>example-branch</code> into <code>main</code> we need first to switch to the main branch :</p>
<div class="sourceCode" id="cb9" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb9-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> switch main</span></code></pre></div>
<p>and then merge :</p>
<div class="sourceCode" id="cb10" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb10-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> merge example-branch</span></code></pre></div>
<p>If we want to merge the two branches without switching to main we can do so using this command :</p>
<div class="sourceCode" id="cb11" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb11-1"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> merge source destination</span>
<span id="cb11-2"></span>
<span id="cb11-3"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">git</span> merge example-branch main </span></code></pre></div>
<p>Note : the source being the first branch (order is important)</p>


</section>

<a onclick="window.scrollTo(0, 0); return false;" id="quarto-back-to-top"><i class="bi bi-arrow-up"></i> Back to top</a> ]]></description>
  <category>course</category>
  <category>notes</category>
  <category>git</category>
  <guid>https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git/working_with_branches.html</guid>
  <pubDate>Sun, 05 Jan 2025 23:00:00 GMT</pubDate>
  <media:content url="https://www.aminehochlef.github.io/posts/06-01-2025-notes-from-datacamp-course-intermediate-git/course_complete_git.png" medium="image" type="image/png" height="75" width="144"/>
</item>
<item>
  <title>Welcome to my Mental Mosaic</title>
  <dc:creator>Amine Hochlef</dc:creator>
  <link>https://www.aminehochlef.github.io/posts/01-10-2024-welcome-to-my-mental-mosaic/</link>
  <description><![CDATA[ 





<p>In the past year or so, I have become very interested in machine learning and its applications. This led me to self-learn it alongside my bachelor’s degree in computer science. I had heard about “learning in public” and sharing what you learn with others before I began this journey, but I thought to myself, “This seems like a lot of trouble.”</p>
<p>Fast forward seven months, and I regret not creating a blog and posting things online.</p>
<section id="what-this-blog-will-contain" class="level1">
<h1>What This Blog Will Contain</h1>
<p>Since this is a technical blog, I won’t be writing about my activities outside of my professional career. The blog will contain posts about machine learning, things I’ve learned, fun projects I’ve worked on, tools I find interesting, and quirky observations about our cool planet—you get the idea.</p>
<p>My goal is to write posts about anything interesting I come across, reflections after a talk or conference, and maybe more.</p>
</section>
<section id="thoughts" class="level1">
<h1>Thoughts</h1>
<p>As a beginner writer who’s just starting to take steps into learning in public, I will prioritize this effort by committing to writing at least one post per month.</p>
<p>Let the fun begin!</p>


</section>

<a onclick="window.scrollTo(0, 0); return false;" id="quarto-back-to-top"><i class="bi bi-arrow-up"></i> Back to top</a> ]]></description>
  <category>Blog</category>
  <guid>https://www.aminehochlef.github.io/posts/01-10-2024-welcome-to-my-mental-mosaic/</guid>
  <pubDate>Mon, 30 Sep 2024 22:00:00 GMT</pubDate>
  <media:content url="https://www.aminehochlef.github.io/posts/01-10-2024-welcome-to-my-mental-mosaic/utopia.jpg" medium="image" type="image/jpeg"/>
</item>
</channel>
</rss>
